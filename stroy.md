> 這是一個關於在嚴苛限制中尋找資訊自由的故事，它不僅是技術上的挑戰，更是一場對網路邊界的探索。**GTunnel 專案**。

---

# 專案介紹：GTunnel — 白名單中的資訊隧道

## 始於困境：數位高牆下的孤島體驗

故事始於一個尋常的連線嘗試，卻遇到了極不尋常的網路環境。當裝置連上一個需要登入認證的 Wi-Fi 熱點時，傳統的網路功能瞬間消失：**App Store** 無法使用、**Gmail** 拒絕同步、一般的網頁瀏覽被無情地重定向或阻擋，甚至連最常見的繞過工具 **VPN** 也無法建立連線。我們發現自己被困在一個由網路管理員精心構築的「**數位高牆 (Walled Garden)**」之中。

最初的挫折是巨大的——這幾乎是一個完全隔絕的網路。但很快，我們發現了這座高牆上的第一道裂痕：某些 Google 服務，例如 **YouTube App** 的影音串流和與 **Gemini ** 的即時通訊，竟然可以正常運作。

## 探索裂縫：白名單的誕生

這個反常的現象引出了核心假設：這不是一個完全關閉的網路，而是一個**精準的流量白名單 (Traffic Whitelisting)**。網路提供者選擇性地允許了部分 **Google 生態系統**的流量通過。

然而，探索的過程充滿了技術上的困擾：

### ❌ 遭遇挫折：與瀏覽器的戰爭

最大的挑戰來自於瀏覽器的行為。當嘗試使用 Chrome 或 Safari 訪問 Google 服務時，**強制門戶 (Captive Portal) 的登入頁面**會不斷彈出，阻止任何進一步的操作。更令人沮喪的是，這個提示不是一次性的，它會每隔一段時間（大約半小時）就重新出現，將連線打回原形。

### ✨ 小發現：穩定的密碼

在反覆的嘗試中，我們發現了穩定連線的關鍵：

> **拒絕登入提示。**

當瀏覽器彈出登入頁面時，只要選擇「拒絕」或關閉它，瀏覽器就會停止重定向，允許先前被白名單放行的 Google 流量繼續傳輸。這個手動、且需要週期性重複的操作，成為了維持連線的**秘密開關**。這證明網路的過濾器是獨立於瀏覽器前端的，而我們只要**穩定瀏覽器的狀態**，就能利用這個通道。

## 繪製地圖：從點到面的資訊隧道

確認了連線的穩定性後，我們開始系統性地繪製這張白名單地圖：

1.  **核心驗證：** Google 搜尋、Google 地圖成功運作。
2.  **技術深度：** 意外發現 **Firebase**（Google 的應用程式後端服務）也能連線。這項發現極具價值，證明白名單不僅限於用戶端的 App，還包含了應用程式底層的 **API 和數據同步服務**，為未來的專案提供了數據傳輸的潛力。
3.  **關鍵突破：** 最重要的發現是 **Google 翻譯**。當我們嘗試讓它翻譯外部的非白名單網頁時，它竟然成功了！這證明 Google 翻譯的伺服器可以充當一個**有限的「間接代理」**，透過白名單的通道為我們獲取外部資訊。

雖然這不是完美的，只能閱讀、不能互動，且排版混亂，但它證明了 **「資訊擷取與轉譯」的可行性**。

## 專案誕生：GTunnel 的使命

經過這段充滿挑戰和發現的旅程，我們意識到手動依靠「拒絕登入」和「Google 翻譯」來獲取資訊是低效且不可持續的。

**GTunnel 專案**應運而生，它的使命是將這些手動、不穩定的步驟，轉化為一個自動化的、高效能的系統：

**GTunnel (Google Tunnel)** 將利用 Gemini 的自然語言處理能力作為使用者介面（模擬 **-cli**），並結合所有白名單上的 Google 服務（特別是 **Firebase** 的穩定傳輸能力），來實現一個完整的「**資訊擷取與轉譯系統**」。

這是一個關於在最受限的網路環境下，透過技術的巧思與不懈的探索，最終為自己開闢出一條資訊自由隧道的專案。它不僅是技術的實踐，更是對網路限制的一種優雅且聰明的回應。